
sources:
  - name: iec_dv2
    schema: edh_unreg_silver_dev_st.bronze
    tables:
      - name: conducting_equipment
        identifier: conducting_equipment
        # columns:
        #   - name: room_type
        #     data_tests:
        #       - dbt_expectations.expect_column_distinct_count_to_equal:
        #           arguments:
        #             value: 4
          # - name: price
          #   data_tests:
          #     - dbt_expectations.expect_column_values_to_match_regex:
          #         arguments:
          #           regex: "^\\\\$[0-9][0-9\\\\.]+$"
      - name: connectivity_nodes
        identifier: connectivity_nodes

      - name: terminals
        # identifier: terminals ( the actual name, if omitted than the name will be taken)
        # config:
        #   loaded_at_field: date
        #   freshness:
        #     warn_after: {count: 1, period: hour}
        #     error_after: {count: 24, period: hour}
